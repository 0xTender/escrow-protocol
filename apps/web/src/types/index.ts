export enum EscrowState {
  NONE,
  BEGUN,
  COMPLETED,
  CANCELLED,
}

export enum AgreementStatus {
  Active = "Active",
  Cancelled = "Cancelled",
  Completed = "Completed",
  Expired = "Expired",
}
